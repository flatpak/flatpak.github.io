+++
title = "CentOS Stream"
description = "CentOS Stream Quick Setup"
template = "setup-distro.html"
aliases = ["/setup/CentOS Stream/", "/setup/CentOS Stream.html"]

[extra]
logo = "centos.svg"
introduction = '''<p>Flatpak is installed by default on CentOS Stream 8 and newer, when using GNOME. To get started, all you need to do is enable Flathub, which is the best way to get Flatpak apps. Just download and install the <a class="btn btn-default" href="https://dl.flathub.org/repo/flathub.flatpakrepo">Flathub repository file</a>.</p> <p>Now all you have to do is <a href="https://flathub.org/">install apps</a>!</p>'''
+++
